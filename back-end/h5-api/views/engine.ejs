<html>

<head>
  <meta charset="UTF-8">
  <title></title>
  <meta content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no" name="viewport">
  <meta content="yes" name="apple-mobile-web-app-capable">
  <meta content="black" name="apple-mobile-web-app-status-bar-style">
  <meta content="telephone=no" name="format-detection">
  <meta content="email=no" name="format-detection">
  <script src="https://cdn.bootcss.com/jquery/3.4.1/jquery.min.js"></script>
  <script src="/third-libs/vue.js"></script>
  <script type="text/javascript" src="/third-libs/flexible.js"></script>
  <link rel="stylesheet" href="/third-libs/animate.min.css">
  <link rel="stylesheet" href="/third-libs/swiper.min.css">
  <link href="https://cdn.bootcss.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
  <script src="https://cdn.bootcss.com/layer/3.0.1/layer.min.js"></script>
  <link href="https://cdn.bootcss.com/layer/3.0.1/skin/default/layer.css" rel="stylesheet">
  <script src="/third-libs/swiper.min.js"></script>
  <!-- <script src="https://cdn.jsdelivr.net/npm/vue"></script> -->
  <!-- <script type="text/javascript" src="http://g.tbcdn.cn/mtb/lib-flexible/0.3.4/flexible.js"></script> -->
  <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/Swiper/4.5.0/css/swiper.min.css"> -->
  <!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/Swiper/4.5.0/js/swiper.min.js"></script> -->
 

  <!-- <script>
  !function(e){function t(a){if(i[a])return i[a].exports;var n=i[a]={exports:{},id:a,loaded:!1};return e[a].call(n.exports,n,n.exports,t),n.loaded=!0,n.exports}var i={};return t.m=e,t.c=i,t.p="",t(0)}([function(e,t){"use strict";Object.defineProperty(t,"__esModule",{value:!0});var i=window;t["default"]=i.flex=function(normal,e,t){var a=e||100,n=t||1,r=i.document,o=navigator.userAgent,d=o.match(/Android[\S\s]+AppleWebkit\/(\d{3})/i),l=o.match(/U3\/((\d+|\.){5,})/i),c=l&&parseInt(l[1].split(".").join(""),10)>=80,p=navigator.appVersion.match(/(iphone|ipad|ipod)/gi),s=i.devicePixelRatio||1;p||d&&d[1]>534||c||(s=1);var u=normal?1:1/s,m=r.querySelector('meta[name="viewport"]');m||(m=r.createElement("meta"),m.setAttribute("name","viewport"),r.head.appendChild(m)),m.setAttribute("content","width=device-width,user-scalable=no,initial-scale="+u+",maximum-scale="+u+",minimum-scale="+u),r.documentElement.style.fontSize=normal?"50px": a/2*s*n+"px"},e.exports=t["default"]}]);  flex(false,100, 1);
  </script> -->

 <script>
    
    // function setDocFontSize(base) {
    //   var width = 375,
    //   domEle = document.documentElement;
    //   dw = domEle.clientWidth || width,
    //   iw = window.innerWidth || width,
    //   sw = window.screen.width || iw,
    //   saw = window.screen.availWidth || iw,
    //   w = Math.min(dw, iw, sw, saw);
    //   base = base || 750;
    //   if (w > base) {
    //       w = base;
    //   }
    //   if (window.devicePixelRatio) {
    //       dpr = window.devicePixelRatio;
    //   } else {
    //       dpr = navigator.userAgent.match(/iphone/i) ? (w > 818 ? 3 : w > 480 ? 2 : 1) : 1;
    //   }
    //   dpr = Math.floor(dpr);
    //   // domEle.style.fontSize = w / base * 100 + "px";
    //   // domEle.body.style.fontSize = w / base * 100 + "px";
    //   domEle.dataset.dpr = dpr;
    // }
    // setDocFontSize(320)
  </script>
  <script>
     let wid = $(window).width();
     let std = wid/400
    let ss = <%- JSON.stringify(work) %>;
    var wists;
    let t = ss.pages[0].elements.length;
    // console.log('___',ss)
    for(i=0;i<t;i++){
      // ss.pages[0].elements[i].commonStyle.width = ss.pages[0].elements[i].commonStyle.width>300 ? wid : ss.pages[0].elements[i].commonStyle.width;
      ss.pages[0].elements[i].commonStyle.width = ss.pages[0].elements[i].commonStyle.width * std >wid ? wid:ss.pages[0].elements[i].commonStyle.width * std ;
      ss.pages[0].elements[i].commonStyle.top = ss.pages[0].elements[i].commonStyle.top * std ;
      ss.pages[0].elements[i].commonStyle.bottom = ss.pages[0].elements[i].commonStyle.bottom * std ;
      ss.pages[0].elements[i].commonStyle.fontSize = ss.pages[0].elements[i].commonStyle.fontSize * std ;
      ss.pages[0].elements[i].commonStyle.height = ss.pages[0].elements[i].commonStyle.height * std ;
      ss.pages[0].elements[i].commonStyle.left = ss.pages[0].elements[i].commonStyle.left * std ;
      
    }
    window.__work = ss

    $('title').html(window.__work.title)

  </script>
  <script src="/engine-assets/engine.umd.js"></script>
  <style>
    * {
      outline: none;
    }
    html,body{
      width:100%;
      overflow-x: hidden;
    }
    .swiper-container {
      width: 100%;
      height: 100%;
    }
    /* .img{
      width:100%!important;
      height: 100%;
    } */
    ul{
      position: fixed!important;
      width:100%!important;
      top:auto!important;
      height: auto!important;
      left:0!important;
      z-index: 999;
    }
    .linkway li i{
      vertical-align: middle;
      margin: 10px 0 0;
    }
    .linkway li p{
      margin: 10px 0;
    }
    .van-swipe__indicators{
      bottom: auto;
    }
   
 
  </style>
</head>
<!-- <body onload="setDocFontSize(320)"> -->
<body>
  <div id="app">
    <!-- <button class="swiper-button-next" style="position: fixed;left: 1000px">Next</button>
    <button class="swiper-button-prev" style="position: fixed;left: 1000px;margin:20px 0">Prev</button> -->
    <engine />
  </div>
  <script>
    // Vue.component('engine', window.Engine)
    new Vue({
      el: '#app',
      data: {
        message: 'Hello Vue!'
      },

    });
  </script>
  <script>
    let imgBox = document.getElementsByTagName('img');
    for(let i=0; i<imgBox.length;i++){
      document.getElementsByTagName('img')[i].parentNode.style.width = '100%';
      document.getElementsByTagName('img')[i].style.width = '100%'
    }
    let item = document.getElementsByClassName('van-swipe');
    for(let i=0;i<item.length;i++){
      document.getElementsByClassName('van-swipe')[i].style.width = '100%';
      document.getElementsByClassName('van-swipe')[i].parentNode.style.width = '100%';
      document.getElementsByClassName('van-swipe')[i].parentNode.style.left = 0
    }

    let contentBox= document.getElementsByClassName('swiper-slide')[0].children[0].children;
    for(let i =0;i<contentBox.length;i++){
      let curWidth = document.getElementsByClassName('swiper-slide')[0].children[0].children[i].clientWidth;
      if(curWidth == 315){
        document.getElementsByClassName('swiper-slide')[0].children[0].children[i].style.width = '100%';
        document.getElementsByClassName('swiper-slide')[0].children[0].children[i].setAttribute("class","full");
        document.getElementsByClassName('full')[0].children[0].style.width = '100%';
      }
     
    let input=document.getElementsByTagName('input');
    console.log('***',input.length)
   for(let x =0;x<input.length;x++){
     document.getElementsByTagName('input')[x].style.width = '100%'
   }
   $('.linkway').parent('div').css('height','auto');
  //  $('.flat').children('div').children('div').css({
  //    'position':'static','top':'auto','bottom':'auto'
  //  })
   $('.flat').children('div').children('div:first-child').hide();
   $('.flat').children('div').children('div:last-child').css('padding','0 0 50px')
   $('.input_group').parent('div').css({'width':'100%','height':'auto'})
   $('.input_group').css({'width':'100%','height':'auto'})
   $('img').css('height','auto')
  //  $('img').parent('div').css({
  //    'position':'static',
  //    'height':'auto',
  //    'top':'auto'
  //  })
   $('.van-swipe__indicators').hide();
   
    let w = $(window).width();
   console.log('当前屏幕款第',w)
   $('.lbt').css('width',w)
   $('.van-swipe-item').css('width',w)
    }
    let input=document.getElementsByTagName('input');
    console.log('***',input.length)
   for(let x =0;x<input.length;x++){
     document.getElementsByTagName('input')[x].style.width = '100%'
   }
   $('.linkway').parent('div').css('height','auto');
  //  $('.flat').children('div').children('div').css({
  //    'position':'static','top':'auto','bottom':'auto'
  //  })
   $('.flat').children('div').children('div:first-child').hide();
   $('.flat').children('div').children('div:last-child').css('padding','0 0 50px')
   $('.input_group').parent('div').css({'width':'100%','height':'auto'})
   $('.input_group').css({'width':'100%','height':'auto'})
   $('img').css('height','auto')
  //  $('img').parent('div').css({
  //    'position':'static',
  //    'height':'auto',
  //    'top':'auto'
  //  })

   //轮播图
  
 



    //let slb = document.getElementsByClassName('full')[0].children;
    //if(document.getElementsByClassName('full')[0].children.length){
    //  for(let x =0; x<slb.length;x++){
    //    document.getElementsByClassName('full')[0].children[x].style.width='100%'
    //  }
    //}
    // var mySwiper = new Swiper('.swiper-container', {
    //   // Optional parameters
    //   direction: 'vertical',
    //   loop: false,
    //   passiveListeners : false,
    //   autoplayDisableOnInteraction : false,
    //   // If we need pagination
    //   pagination: {
    //     el: '.swiper-pagination',
    //     clickable:true,
    //   },
    //   pagination : '.swiper-pagination',
    //   paginationClickable: true,
    //   longSwipesRatio: 0.3,
    //   touchRatio:1,
    //   observer:true,//修改swiper自己或子元素时，自动初始化swiper
    //   observeParents:true,//修改swiper的父元素时，自动初始化swiper
    //   // Navigation arrows
    //   // navigation: {
    //   //   nextEl: '.swiper-button-next',
    //   //   prevEl: '.swiper-button-prev',
    //   // },
    //   // And if we need scrollbar
    //   scrollbar: {
    //     el: '.swiper-scrollbar',
    //   },
    // });

    // function displayMessage ({ origin, data }) {
    //   if (['next', 'prev'].includes(data)  && origin === window.location.origin) {
    //     document.querySelector(`.swiper-button-${data}`).click()
    //   }
    // }

    // if (window.addEventListener) {
    //   window.addEventListener("message", displayMessage, false);
    // } else {
    //   window.attachEvent("onmessage", displayMessage);
    // }
    // document.body.addEventListener('', function(event) {
    //             event = event ? event : window.event;
    //             if(event.preventDefault) {
    //                 event.preventDefault()
    //             } else {
    //                 event.returnValue = false
    //             }
    //         }, { passive: false })

  </script>
  <!-- build:js scripts/vendor.js -->
  <!-- endbuild -->
</body>

</html>